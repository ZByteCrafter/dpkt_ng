# Batch 4: IEEE 802.11 修复计划

**Goal:** 修复 ieee80211_ie.py (6 Critical, 4 Minor, 1 Style) + ieee80211.py (1 Critical, 2 Major, 4 Minor)

**Files:** `dpkt/ieee80211_ie.py`, `dpkt/ieee80211.py`

---

## Part A: ieee80211_ie.py

### Task 1: 修复 HE Capabilities 字段长度 (Critical)

- [ ] **Step 1: 修改 IEEE80211IEHECapability.unpack (line 100-103)**

```python
# 修改前:
if len(self.info) >= 6:
    self.mac_info = self.info[1:6]       # 5 bytes (BUG: should be 6)
if len(self.info) >= 17:
    self.phy_info = self.info[6:17]      # 11 bytes, wrong start

# 修改后 (IEEE 802.11-2020 §9.4.2.312: MAC=6B, PHY=11B):
if len(self.info) >= 7:
    self.mac_info = self.info[1:7]       # 6 bytes
if len(self.info) >= 18:
    self.phy_info = self.info[7:18]      # 11 bytes, correct start
```

- [ ] **Step 2: 更新测试**

```python
def test_he_cap_ie():
    """HE Capabilities with correct field sizes."""
    # ext_id(1) + mac_cap(6) + phy_cap(11) = 18 bytes
    info = bytes([HE_EXT_CAP]) + b'\x00' * 6 + b'\x00' * 11
    buf = bytes([255, len(info)]) + info
    ie = IEEE80211IEHECapability(buf)
    assert len(ie.mac_info) == 6
    assert len(ie.phy_info) == 11
```

- [ ] **Step 3: 提交** `git commit -m "fix: correct HE Capabilities MAC field to 6 bytes"`

### Task 2: 修复 BSS Color Change 字段顺序 (Critical)

- [ ] **Step 1: 交换 countdown 和 new_color (line 131-134)**

```python
# 修改前:
self.new_color = self.info[1]    # BUG: this is countdown
self.countdown = self.info[2]    # BUG: this is new_color

# 修改后 (IEEE 802.11-2020 §9.4.2.319):
self.countdown = self.info[1]    # Color Switch Countdown
self.new_color = self.info[2]    # New BSS Color
```

- [ ] **Step 2: 更新测试**

```python
def test_bss_color_change_ie():
    info = bytes([HE_EXT_BSS_COLOR]) + bytes([10]) + bytes([5])
    buf = bytes([255, len(info)]) + info
    ie = IEEE80211IEHEBSSColorChange(buf)
    assert ie.countdown == 10    # was new_color
    assert ie.new_color == 5     # was countdown
```

- [ ] **Step 3: 提交** `git commit -m "fix: swap BSS Color Change countdown/new_color field order"`

### Task 3: 修复 HE 6 GHz Band 越界读取 (Critical)

- [ ] **Step 1: 修复长度检查 (line 153)**

```python
# 修改前:
if len(self.info) >= 5:    # BUG: need 6 for struct.unpack('<I', ...)

# 修改后:
if len(self.info) >= 6:
```

- [ ] **Step 2: 提交** `git commit -m "fix: correct HE 6 GHz Band length check to >= 6"`

### Task 4: 修复 EHT Capabilities 字段长度 (Critical)

- [ ] **Step 1: 修改 IEEE80211IEEHTCapability.unpack (line 173-178)**

```python
# 修改前:
if len(self.info) >= 5:
    self.mac_cap = self.info[1:5]        # 4 bytes (BUG: should be 2)
if len(self.info) >= 13:
    self.phy_cap = self.info[5:13]       # 8 bytes (BUG: should be 9)
self.eht_mcs_nss = self.info[13:]

# 修改后 (802.11be §9.4.2.313: MAC=2B, PHY=9B):
if len(self.info) >= 3:
    self.mac_cap = self.info[1:3]        # 2 bytes
if len(self.info) >= 12:
    self.phy_cap = self.info[3:12]       # 9 bytes
self.eht_mcs_nss = self.info[12:]       # variable
```

- [ ] **Step 2: 更新测试**

```python
def test_eht_cap_ie():
    # ext_id(1) + mac_cap(2) + phy_cap(9) = 12 bytes
    info = bytes([EHT_EXT_CAP]) + b'\x00' * 2 + b'\x00' * 9
    buf = bytes([255, len(info)]) + info
    ie = IEEE80211IEEHTCapability(buf)
    assert len(ie.mac_cap) == 2
    assert len(ie.phy_cap) == 9
```

- [ ] **Step 3: 提交** `git commit -m "fix: correct EHT Capabilities field sizes (MAC=2B, PHY=9B)"`

### Task 5: 修复 ML Element 位提取 (Critical)

- [ ] **Step 1: 修正位移 (line 209-210)**

```python
# 修改前:
self.type = (self.ml_control >> 0) & 0x7     # BUG: includes Extension bit
self.presence = (self.ml_control >> 3)        # BUG: wrong position

# 修改后 (802.11be: B0=Ext, B1-B3=Type, B4=Pres):
self.type = (self.ml_control >> 1) & 0x7
self.presence = (self.ml_control >> 4) & 0x1
```

- [ ] **Step 2: 更新测试**

```python
def test_ml_element():
    info = bytes([EHT_EXT_ML_COMMON]) + struct.pack('<H', 0x0001)
    buf = bytes([255, len(info)]) + info
    ie = IEEE80211IEMLElement(buf)
    assert ie.type == 0       # was 1 (Extension bit bled into Type)
    assert ie.presence == 0
```

- [ ] **Step 3: 提交** `git commit -m "fix: correct ML Element bit extraction (Type, Presence)"`

### Task 6: 修复 VHT Operation ch2 字段宽度 (Critical)

- [ ] **Step 1: 修改 IEEE80211IEVHTOperation.unpack (line 253-257)**

```python
# 修改前:
if len(self.info) >= 5:
    self.ch_width = self.info[0]
    self.ch1 = self.info[1]
    self.ch2 = struct.unpack('<H', self.info[2:4])[0]     # BUG: 2 bytes, should be 1
    self.basic_mcs = struct.unpack('<H', self.info[4:6])[0]

# 修改后 (IEEE 802.11-2020 §9.4.2.159):
if len(self.info) >= 5:
    self.ch_width = self.info[0]
    self.ch1 = self.info[1]
    self.ch2 = self.info[2]                                # 1 byte
    self.basic_mcs = struct.unpack('<H', self.info[3:5])[0]
```

- [ ] **Step 2: 提交** `git commit -m "fix: VHT Operation ch2 is 1 byte, not 2"`

---

## Part B: ieee80211.py

### Task 7: 修复 BlockAck data 截断 (Critical)

- [ ] **Step 1: 修改 line 365**

```python
# 修改前:
self.data = self.data[len(self.__hdr__) + len(self.bmp):]  # BUG: __hdr__ returns field count

# 修改后:
self.data = self.data[len(self.bmp):]
```

- [ ] **Step 2: 提交** `git commit -m "fix: BlockAck data truncation - use bmp length only"`

### Task 8: 添加缺失的 __byte_order__ (Major)

- [ ] **Step 1: 为以下类添加 `__byte_order__ = '<'`**

需要修改的类（均在 ieee80211.py 中）:
- `BlockAckReq` (line ~308)
- `RTS` (line ~376)
- `CTS` (line ~382)
- `ACK` (line ~387)
- `CFEnd` (line ~392)
- `Disassoc` (line ~415)
- `Auth` (line ~451)
- `Deauth` (line ~457)
- `Action` (line ~462)
- `BlockAckActionRequest` (line ~489)
- `BlockAckActionResponse` (line ~497)
- `QoS_Data` (line ~550)

每个类添加:
```python
class BlockAckReq(dpkt.Packet):
    __byte_order__ = '<'  # IEEE 802.11 is little-endian
    __hdr__ = (...)
```

- [ ] **Step 2: 提交** `git commit -m "fix: add __byte_order__='< ' to 12 IEEE 802.11 inner classes"`

### Task 9: 修复 unpack_ies 循环条件 (Major)

- [ ] **Step 1: 修改 line 176**

```python
# 修改前:
while len(buf) > FCS_LENGTH:  # BUG: FCS already stripped

# 修改后:
while len(buf) >= 2:  # Minimum IE is 2 bytes (id + len)
```

- [ ] **Step 2: 提交** `git commit -m "fix: IEEE 802.11 unpack_ies loop condition to >= 2"`
